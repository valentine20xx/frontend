import { LastComaFirstNamePipe } from './last-coma-first-name.pipe';

describe('LastComaFirstnamePipe', () => {
  it('create an instance', () => {
    const pipe = new LastComaFirstNamePipe();
    expect(pipe).toBeTruthy();
  });
});
